if type -q bat
    abbr cat bat
end


if type -q doggo
    abbr dig doggo
end

if type -q git
    abbr --command git pl pull --rebase --autostash
    abbr --command git pf push --force
end

if type -q kubectl
    abbr k kubectl
end
