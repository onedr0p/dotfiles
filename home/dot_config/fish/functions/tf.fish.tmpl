function tf --wraps=terraform --description 'terraform shorthand'
    {{- if eq (includeTemplate "machine" .) "termux" }}
    proot -b $PREFIX/etc/resolv.conf:/etc/resolv.conf terraform $argv
    {{- else }}
    command terraform $argv
    {{- end }}
end
